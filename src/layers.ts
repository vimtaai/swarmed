import { Layer } from "./classes/layer";

export const background = new Layer("background");
export const foreground = new Layer("foreground");
export const overlay = new Layer("overlay");
export const ui = new Layer("ui");
